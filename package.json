{
  "name": "hangho-pos",
  "version": "1.0.0",
  "private": true,
  "description": "Hangho POS — Quản lý bán hàng offline, không cần internet",
  "scripts": {
    "dev": "npm run dev:legacy",
    "dev:legacy": "npx serve public/legacy -p 1420 --no-clipboard",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
